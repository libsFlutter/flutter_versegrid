name: flutter_versegrid
description: >-
  Shared Flutter widgets for verse and shloka presentation (passages,
  translations, range chips).
version: 0.1.0
homepage: https://github.com/libsFlutter/flutter_versegrid
repository: https://github.com/libsFlutter/flutter_versegrid

environment:
  sdk: ^3.11.5
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
